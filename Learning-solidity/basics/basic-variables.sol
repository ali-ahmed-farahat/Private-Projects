pragma solidity >=0.6.0 < 0.9.0;

contract SimpleStorage{

//basic variable types

    uint256 favouriteNum = 5;  //if I wrote uint256 favouriteNum; it getrs intialized to the null value (0)
    bool favouriteBool = true;
    string favString = "Ali";
    address favAddress = 0x2B148F2E8205838Dd0c1BB9B3F9E313d7F350525;
    bytes32 favByte = "cats are cool";
}
